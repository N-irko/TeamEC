<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.co.internous.framepj.model.mapper.MstProductMapper">

	<select id="findByCategoryAndProductName" resultType="jp.co.internous.framepj.model.domain.MstProduct">
    SELECT
        id, product_name, price, image_full_path
    FROM
        mst_product
    WHERE
        1 = 1
        <if test="category != 0">
            AND category_id = #{category}
        </if>
        <foreach collection="keywords" item="keyword" open="AND (" separator="OR" close=")">
            product_name LIKE CONCAT('%', #{keyword}, '%')
        </foreach>
</select>
	
	<select id="findByProductName" resultType="jp.co.internous.framepj.model.domain.MstProduct">
		select
		p.id,
		p.product_name, p.price, p.image_full_path, c.category_name
		from
		mst_product AS p
		INNER JOIN
		mst_category AS c
		ON 
		p.category_id = c.id
		where
		product_name = like%#{keywords}%;
	</select>
	
</mapper>